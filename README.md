# Анализатор Производительности Солнечных Данных

## 🎯 О проекте

**Solar Performance Analyzer** - это демонстрационное приложение обработки изображений солнца

### Основная идея

Приложение загружает реальные изображения Солнца с NASA SOHO (Solar and Heliospheric Observatory) и обрабатывает их двумя способами:
- **Многопоточная обработка** (симуляция GPU) - быстро
- **Однопоточная обработка** (CPU) - медленно

Затем сравнивает производительность и показывает, насколько быстрее работает параллельная обработка.

### Ключевые цифры

- ⚡ **Ускорение в 2.9 раза** при многопоточной обработке
- 📊 **100% успешная обработка** всех данных
- 🌐 **Реальные данные** с NASA SOHO
- 🔄 **Автоматическое восстановление** при ошибках

---

### Применения

Такая же архитектура используется в:

**Финтех** - обработка биржевых данных в реальном времени  
**E-commerce** - fraud detection, анализ заказов  
**Стриминг** - обработка видео потоков (Netflix, YouTube)  
**IoT** - анализ данных с датчиков  
**Мониторинг** - обработка метрик и логов

---

## 🔍 Что делает приложение

### Пошаговый процесс

1. **Загрузка данных** (каждые 5 секунд)
   - Подключается к NASA SOHO API
   - Загружает последнее изображение Солнца (EIT 195Å)
   - Размер: ~488 KB
   - При ошибке автоматически переключается на резервный URL

2. **Параллельная обработка**
   - Запускает обработку на "GPU" (многопоточно)
   - Запускает обработку на CPU (однопоточно)
   - Обе обработки идут одновременно

3. **Обработка изображения**
   - Преобразование в оттенки серого
   - Применение оператора Собеля (edge detection)
   - Вычисление градиентов
   - Создание результирующего изображения

4. **Сбор метрик**
   - Время обработки GPU
   - Время обработки CPU
   - Коэффициент ускорения
   - Размер данных

5. **Вывод результатов**
   - Показывает время обработки для каждого метода
   - Вычисляет ускорение (speedup)
   - Выводит финальную статистику

### Пример вывода

```
Frame #1
  GPU Processing: 252.50 ms
  CPU Processing: 497.56 ms
  Speedup: 2.0x
  Image Size: 488 KB
```

---

## ⚙️ Как это работает

### Архитектура системы

```
┌─────────────────────────────────────────────────────────┐
│              SolarAnalyzerConsole (main)                 │
│                   Точка входа                            │
└────────────────────┬────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────┐
│              SolarDataPipeline                           │
│           Реактивный конвейер обработки                  │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Download   │→ │   Process    │→ │   Metrics    │ │
│  │   (Netty)    │  │  (GPU+CPU)   │  │  (Collect)   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
┌───────▼────────┐      ┌────────▼──────────┐
│ NettyImage     │      │  Image Processors │
│ Downloader     │      │                   │
│                │      │  ┌─────────────┐  │
│ - Connection   │      │  │ GPU (Multi) │  │
│   pooling      │      │  │ Processor   │  │
│ - Retry        │      │  └─────────────┘  │
│ - Fallback     │      │  ┌─────────────┐  │
│                │      │  │ CPU (Single)│  │
│                │      │  │ Processor   │  │
└────────────────┘      │  └─────────────┘  │
                        └───────────────────┘
```

### Детальное описание компонентов



#### 1. SolarAnalyzerConsole (Главный класс)

**Что делает:**
- Инициализирует приложение
- Создает конвейер обработки
- Подписывается на поток данных
- Выводит результаты в консоль

**Ключевой код:**
```java
SolarDataPipeline pipeline = new SolarDataPipeline();
pipeline.getDataStream()
    .take(10)  // Обработать 10 изображений
    .subscribe(
        image -> displayResults(image),  // Показать результат
        error -> handleError(error),     // Обработать ошибку
        () -> displayFinalStats()        // Показать финальную статистику
    );
```

#### 2. SolarDataPipeline (Реактивный конвейер)

**Что делает:**
- Создает поток данных (Flux) с интервалом 5 секунд
- Загружает изображения через Netty
- Запускает параллельную обработку GPU + CPU
- Собирает метрики производительности

**Ключевые особенности:**
- **Неблокирующий** - не ждет завершения операций
- **Реактивный** - обрабатывает данные по мере поступления
- **Устойчивый** - автоматический retry при ошибках
- **Масштабируемый** - может обрабатывать тысячи запросов

**Поток данных:**
```
Timer (5 сек) → Download → Process (GPU + CPU) → Metrics → Output
     ↓              ↓              ↓                ↓          ↓
  Trigger      Netty HTTP    Parallel exec    Lock-free   Console
                + Retry      + Zero-copy      counters
```

#### 3. NettyImageDownloader (Сетевой слой)

**Что делает:**
- Загружает изображения по HTTP
- Использует connection pooling (переиспользование соединений)
- Автоматически следует редиректам
- Оптимизирует TCP соединения

**Оптимизации:**
```java
ConnectionProvider:
  - maxConnections: 10           // Максимум 10 соединений
  - maxIdleTime: 30 секунд       // Время простоя
  - maxLifeTime: 5 минут         // Время жизни соединения

TCP оптимизации:
  - TCP_NODELAY: true            // Отключить алгоритм Nagle
  - SO_KEEPALIVE: true           // Поддерживать соединение
  - Adaptive buffers             // Динамический размер буферов
```

**Почему это быстро:**
- Не создает новое соединение каждый раз (экономия ~150ms)
- Использует прямые буферы (zero-copy)
- Оптимизирует размер буферов под данные

#### 4. GpuImageProcessor (Многопоточная обработка)

**Что делает:**
- Симулирует GPU обработку через многопоточность
- Распределяет работу между всеми ядрами процессора
- Применяет алгоритм edge detection (обнаружение границ)

**Алгоритм:**
```
1. Разделить изображение на части (по количеству ядер)
2. Каждое ядро обрабатывает свою часть параллельно:
   - Преобразование в оттенки серого
   - Применение оператора Собеля
   - Вычисление градиентов
3. Объединить результаты
```

**Почему это быстро:**
- Использует все 8 ядер процессора одновременно
- Каждое ядро работает независимо
- Нет блокировок и ожидания

**Пример:**
```
8 ядер × 125ms на ядро = 125ms общее время
vs
1 ядро × 410ms = 410ms общее время
Ускорение: 410 / 125 = 3.3x
```

#### 5. CpuImageProcessor (Однопоточная обработка)

**Что делает:**
- Обрабатывает изображение последовательно
- Один пиксель за раз
- Используется для сравнения с GPU

**Алгоритм:**
```
Для каждого пикселя (1024×1024 = 1,048,576 пикселей):
  1. Прочитать соседние пиксели
  2. Вычислить градиент по X
  3. Вычислить градиент по Y
  4. Вычислить величину градиента
  5. Записать результат
```

**Почему это медленно:**
- Обрабатывает пиксели по очереди
- Использует только 1 ядро из 8
- Много операций на каждый пиксель

#### 6. PerformanceMetrics (Сбор метрик)

**Что делает:**
- Собирает статистику производительности
- Использует lock-free структуры данных
- Вычисляет среднее, минимум, максимум

**Метрики:**
- Время загрузки
- Время обработки GPU
- Время обработки CPU
- Коэффициент ускорения
- Пропускная способность (frames/sec)

**Почему это быстро:**
- LongAdder вместо AtomicLong (нет конкуренции)
- Нет синхронизации
- Нет блокировок

---

## 🏗️ Архитектура проекта

### Слои приложения

```
┌─────────────────────────────────────────┐
│         Presentation Layer              │
│      (SolarAnalyzerConsole)             │
│  - Вывод в консоль                      │
│  - Форматирование результатов           │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│         Business Logic Layer            │
│       (SolarDataPipeline)               │
│  - Оркестрация процесса                 │
│  - Реактивный поток данных              │
│  - Обработка ошибок                     │
└────────────────┬────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
┌───────▼──────┐  ┌──────▼────────┐
│ Network      │  │ Processing    │
│ Layer        │  │ Layer         │
│              │  │               │
│ - Netty      │  │ - GPU Proc    │
│ - HTTP       │  │ - CPU Proc    │
│ - Pooling    │  │ - Algorithms  │
└──────────────┘  └───────────────┘
        │                 │
┌───────▼─────────────────▼───────┐
│      Infrastructure Layer       │
│  - Metrics (PerformanceMetrics) │
│  - Logging (Logback)            │
│  - Configuration                │
└─────────────────────────────────┘
```

### Паттерны проектирования



#### 1. Reactive Streams Pattern
```
Publisher (Flux) → Subscriber (Console)
  - Backpressure handling
  - Non-blocking I/O
  - Event-driven
```

#### 2. Strategy Pattern
```
ImageProcessor (interface)
  ├── GpuImageProcessor (многопоточная стратегия)
  └── CpuImageProcessor (однопоточная стратегия)
```

#### 3. Observer Pattern
```
SolarDataPipeline (Observable)
  └── SolarAnalyzerConsole (Observer)
```

#### 4. Retry Pattern
```
download() → error → retry → fallback URL → success
```

#### 5. Circuit Breaker Pattern (неявно)
```
Если ошибки продолжаются → retry с экспоненциальной задержкой
```

---

## 🛠️ Технологии

### Основной стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Java** | 21 | Основной язык программирования |
| **Maven** | 3.8+ | Управление зависимостями и сборка |
| **Netty** | 4.1.100 | High-performance HTTP клиент |
| **Project Reactor** | 3.6.0 | Реактивное программирование |
| **SLF4J + Logback** | 2.0.9 | Логирование |
| **JMH** | 1.37 | Бенчмаркинг |

### Почему именно эти технологии?

#### Java 21
- Последняя LTS версия
- Virtual Threads (Project Loom)
- Улучшенная производительность GC
- Современные API

#### Netty
- Используется в: Cassandra, Elasticsearch, gRPC
- В 3-5 раз быстрее стандартного HttpClient
- Connection pooling из коробки
- Zero-copy операции

#### Project Reactor
- Используется в: Spring WebFlux, Netflix
- Неблокирующий I/O
- Backpressure handling
- Композиция асинхронных операций

#### Maven
- Стандарт индустрии
- Огромная экосистема плагинов
- Декларативная конфигурация

---

## 🚀 Установка и запуск

### Требования

- **Java 21** или выше
- **Maven 3.8+**
- **Интернет-соединение** (для загрузки данных с NASA)

### Проверка установки

```bash
# Проверить Java
java -version
# Должно показать: java version "21.0.x"

# Проверить Maven
mvn -version
# Должно показать: Apache Maven 3.8.x или выше
```

### Сборка проекта

```bash
# 1. Перейти в директорию проекта
cd solar-performance-analyzer

# 2. Собрать проект
mvn clean package

# Ожидаемый результат:
# [INFO] BUILD SUCCESS
# [INFO] Total time: 30 s
```

### Запуск приложения

#### Через Maven

```bash
mvn exec:java -Dexec.mainClass="com.solarlab.SolarAnalyzerConsole"
```

### Что произойдет при запуске

1. **Инициализация** (2-3 секунды)
   ```
   ================================================================================
   Solar Performance Analyzer - Console Mode
   ================================================================================
   Java Version: 21.0.6
   Available Processors: 8
   Max Memory: 4046 MB
   ```

2. **Загрузка первого изображения** (~3 секунды)
   ```
   Starting data pipeline...
   Will process 10 images and show performance comparison
   ```

3. **Обработка и вывод результатов** (каждые 5 секунд)
   ```
   Frame #1
     GPU Processing: 252.50 ms
     CPU Processing: 497.56 ms
     Speedup: 2.0x
     Image Size: 488 KB
   ```

4. **Финальная статистика** (после 10 кадров)
   ```
   FINAL PERFORMANCE STATISTICS
   Total Frames Processed: 10
   Total Data Downloaded: 4.77 MB
   GPU Performance: Average Latency: 140.34 ms
   CPU Performance: Average Latency: 410.21 ms
   Overall Speedup: 2.9x
   ```

### Остановка приложения

Нажмите `Ctrl+C` в консоли.

---

## 📊 Результаты работы

### Реальные метрики (протестировано 19.11.2025)



| Метрика | Значение | Описание |
|---------|----------|----------|
| **Обработано кадров** | 10 | Все успешно |
| **Загружено данных** | 4.77 MB | С NASA SOHO |
| **GPU латентность** | 140.34 ms | Многопоточная обработка |
| **CPU латентность** | 410.21 ms | Однопоточная обработка |
| **Ускорение** | **2.9x** | GPU быстрее CPU |
| **Успешность** | **100%** | Нет потерянных кадров |
| **Обработано ошибок** | 3 | Все восстановлены |

### Детальная статистика по кадрам

```
Кадр  | GPU (ms) | CPU (ms) | Ускорение | Размер
------|----------|----------|-----------|--------
#1    | 252.50   | 497.56   | 2.0x      | 488 KB
#2    | 122.78   | 394.68   | 3.2x      | 488 KB
#3    | 141.48   | 447.63   | 3.2x      | 488 KB
#4    | 130.48   | 391.39   | 3.0x      | 488 KB
#5    | 130.15   | 469.62   | 3.6x      | 488 KB
#6    | 134.57   | 375.01   | 2.8x      | 488 KB
#7    | 134.94   | 374.53   | 2.8x      | 488 KB
#8    | 144.32   | 451.15   | 3.1x      | 488 KB
#9    | 104.45   | 368.05   | 3.5x      | 488 KB
#10   | 107.69   | 332.47   | 3.1x      | 488 KB
------|----------|----------|-----------|--------
Сред. | 140.34   | 410.21   | 2.9x      | 488 KB
```

### Анализ производительности

#### GPU (многопоточная обработка)
- **Первый кадр:** 252.50 ms (холодный старт, инициализация потоков)
- **После прогрева:** 104-144 ms (стабильная работа)
- **Среднее:** 140.34 ms
- **Использование:** Все 8 ядер процессора

#### CPU (однопоточная обработка)
- **Первый кадр:** 497.56 ms
- **После прогрева:** 332-469 ms
- **Среднее:** 410.21 ms
- **Использование:** 1 ядро процессора

#### Ускорение
- **Минимум:** 2.0x (холодный старт)
- **Максимум:** 3.6x (оптимальные условия)
- **Среднее:** 2.9x
- **Стабильность:** ±0.4x после прогрева

### Обработка ошибок

Во время тестирования произошло 3 сетевых ошибки:
```
[11:43:48] Primary URL failed → Fallback URL failed → Retry → Success
[11:43:58] Primary URL failed → Fallback URL → Success
[11:44:03] Primary URL failed → Fallback URL → Success
```

**Результат:** Все ошибки успешно обработаны, данные не потеряны.

---

## 📁 Структура файлов

### Обзор проекта

```
solar-performance-analyzer/
├── src/
│   ├── main/
│   │   ├── java/com/solarlab/          # Исходный код
│   │   └── resources/                   # Конфигурация
│   └── test/                            # Тесты
├── docs/                                # Документация
├── pom.xml                              # Maven конфигурация
└── README.md                            # Этот файл
```

### Детальная структура

```
src/main/java/com/solarlab/
│
├── SolarAnalyzerConsole.java           # Главный класс приложения
│   └── Назначение: Точка входа, вывод результатов
│
├── core/                                # Ядро приложения
│   ├── SolarDataPipeline.java          # Реактивный конвейер
│   │   └── Назначение: Оркестрация загрузки и обработки
│   │
│   ├── ProcessedImage.java             # Контейнер результатов
│   │   └── Назначение: Хранение обработанных данных + метрики
│   │
│   ├── CpuImageProcessor.java          # CPU обработка
│   │   └── Назначение: Однопоточная обработка изображений
│   │
│   └── FlareDetector.java              # Детектор вспышек
│       └── Назначение: Обнаружение солнечных вспышек
│
├── netty/                               # Сетевой слой
│   └── NettyImageDownloader.java       # HTTP клиент
│       └── Назначение: Загрузка изображений с NASA
│
├── gpu/                                 # GPU обработка
│   └── GpuImageProcessor.java          # Многопоточная обработка
│       └── Назначение: Симуляция GPU через многопоточность
│
├── benchmark/                           # Бенчмаркинг
│   ├── PerformanceMetrics.java         # Сбор метрик
│   │   └── Назначение: Lock-free счетчики производительности
│   │
│   └── PerformanceBenchmark.java       # JMH бенчмарки
│       └── Назначение: Микро-бенчмарки для оптимизации
│
└── util/                                # Утилиты
    └── ImageUtils.java                  # Работа с изображениями
        └── Назначение: Конвертация, heat maps, resize
```